[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "Awesome Docs"
authors = [{name = "Eric", email = "realmaguodong@outlook.com"}]
dynamic = ["version", "description"]
